<%= form_for @ingredient do |r| %>
  <% if @ingredient.errors %>
    <ul>
      <% @ingredient.errors.full_messages.each do |m| %>
        <li><%= m %></li>
      <% end %>
    </ul>
  <% end %>

  <%= r.label :name %>
  <%= r.text_field :name %>

  <br></br>

  <%= r.submit %>
<% end %>
